# sdetExercise

```
// Step 1: Install the test framework in your terminal (Mac) or command line (Windows)
$ npm install bitbucket:carepo/weblab-common-test --save-dev

// Step 2: 
$ npm install

// Step 3: To run the tests
$ npm run test
```
